resources:
  pipelines:
    oms_silver:
      name: tr020_oms_silver
      catalog: ${var.catalog_name}
      schema: ${var.silver_schema}
      photon: true
      serverless: true
      continuous: false
      configuration:
        trade_recon.oms_bronze_table: ${var.catalog_name}.${var.bronze_schema}.oms_trade_event_raw_lakeflow

      libraries:
        - file:
            path: ../../src/trade_recon/pipelines/oms_silver.py